class Solution {
    public boolean stoneGame(int[] piles) {
        return true;
    }
}

/*
   Complexity analysis:
   Time: O(1), as the output is always same or does not depend on the input size.
   Space: O(1), since we we do not use an extra space.
*/
